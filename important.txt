When you create a new AVR platformio project, clangd won't work out of the
box.
You must add:

  build_flags = --target=avr

in your platformio.ini file.
Then, you can remove that flag, otherwise the SW won't build.

Copy the .clang-tidy file from anywhere.
